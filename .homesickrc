def installVundler
  `git clone https://github.com/gmarik/vundle.git ~/.vim/bundle/vundle`
end

def installTmux
  `brew install tmux` unless system 'which tmux'
end

def installZsh
  `brew install zsh` unless system 'which zsh'
end

def installNvm
  #https://github.com/creationix/nvm
  unless system 'which nvm'
    puts 'Installing nvm'
    `curl https://raw.githubusercontent.com/creationix/nvm/v0.22.0/install.sh | bash`
  end
end

def installTheSilverSearcher
  `brew install the_silver_searcher` unless system 'which ag'
end

def installFonts
  #Download fonts from
  #https://github.com/Lokaltog/powerline-fonts/tree/master/SourceCodePro
  #you might need to copy them manually to /Library/Fonts and your name/Library/Fonts
end

def importTerminalColourScheme
  #~/jellybeans.terminal
  #Import profile in to terminal
end

installVundler
installTmux
installZsh
installNvm
installTheSilverSearcher

#UI
installFonts
importTerminalColourScheme

